name = 'guilherme'
texto = 'texto de guilherme'

#Verifica se name está no texto
if name in texto:
    print(texto)

#Verifica se name não está no texto
if not name in texto:
    print(name)
